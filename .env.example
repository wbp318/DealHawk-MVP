# DealHawk Backend Configuration

# Environment: "development" or "production"
# In production: Swagger/ReDoc docs are disabled
ENVIRONMENT=development

# Database
DATABASE_URL=sqlite:///./dealhawk.db
# PostgreSQL (production):
# DATABASE_URL=postgresql://dealhawk:dealhawk@localhost:5432/dealhawk

# Server
API_HOST=0.0.0.0
API_PORT=8000

# Set to true only for local development (enables SQL query logging)
DEBUG=false

# Logging
LOG_LEVEL=INFO

# JWT Authentication
# IMPORTANT: Change this in production! Use a long random string.
JWT_SECRET_KEY=dealhawk-dev-secret-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Stripe (required for subscription billing)
# Get keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Create a Pro price in Stripe Dashboard → Products → Price ID
STRIPE_PRO_PRICE_ID=

# Database migrations (production)
# Run: alembic upgrade head

# Redis / Celery
# REDIS_URL=redis://localhost:6379/0
# CELERY_BROKER_URL=          # defaults to REDIS_URL if empty
# CELERY_RESULT_BACKEND=      # defaults to REDIS_URL if empty

# Email
# EMAIL_PROVIDER=smtp          # "smtp" or "sendgrid"
# SENDGRID_API_KEY=
# SMTP_HOST=localhost
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_USE_TLS=true
# EMAIL_FROM_ADDRESS=noreply@dealhawk.app
# EMAIL_FROM_NAME=DealHawk

# MarketCheck API (optional - uses stub data if not set)
MARKETCHECK_API_KEY=

# Dealership API tier
# IMPORTANT: Change this salt in production!
DEALER_API_KEY_SALT=dealhawk-dealer-key-salt
